package com.example.online_exam.discussion.enums;

public enum FileType {
    IMAGE,      // JPG, PNG, GIF, WEBP
    DOCUMENT,   // PDF, DOC, DOCX, XLS, XLSX, PPT, PPTX, TXT
    VIDEO,      // MP4, WEBM (future)
    OTHER       // ZIP, RAR, etc.
}
